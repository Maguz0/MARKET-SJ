{
  "name": "san-jorge-ventas",
  "version": "1.0.0",
  "description": "App de ventas online para San Jorge y sus alrededores",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Building...'",
    "cap-add": "npx cap add android",
    "cap-sync": "npx cap sync",
    "cap-open": "npx cap open android"
  },
  "dependencies": {
    "firebase": "^10.7.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
